# Clean Implementation Architecture

NORMATIVE_SOURCE: `FORMAL_MODEL.json`  
NORMATIVE_SOURCE_VERSION: `1.0.1`  
DERIVATION_TYPE: human-readable projection

Authority enters through E01 and is transformed by an SOD-bound mandate, never inferred from possession or descendants. E02 resolves canonical target/action semantics; E03 fixes stable business identity; E04 defines the runtime authority domain, transition families, acceptor and distinct closure authority.

Phase-specific typed events establish identity, policy, Human, external, PIP, PEP, revocation and target context. C01 externally closes the exact context history; S01 deterministically projects readiness. Global claim, permit, mediation and consumption proceed through separate typed transitions and stage-specific closure/projection pairs. Global uniqueness belongs to the action-derived EffectSlotId, never an execution-local record.

E31 is a same-attempt, later-phase subordinate Human-authority transformation. It preserves action, domain, slot, SOD, PEP, consumption, revocation and effect-gate requirements. E32 represents typed non-use of break-glass.

Target comparison and complete pre-effect revalidation precede E22. C05 closes the exact effect-authorization history; S05 yields one authorization or deny; E23 is the immediate effect barrier. Effect, outcome and audit follow strictly. Audit creates no authority. Retry/recovery begins a new attempt epoch but retains action/domain/slot and cannot follow a completed effect.
