# Fixed Certification Boundary

NORMATIVE_SOURCE: `CANONICAL_MODEL.json`  
NORMATIVE_SOURCE_VERSION: `1.0.0`  
DERIVATION_TYPE: certification-boundary and convergence projection

Layer 17 is architecturally sufficient when invariants A–X hold in every valid abstract admission history, all 24 fixed falsification classes and every applicable historical counterexample fail closed, exact predecessor and authority mappings reconcile, the graph is complete and acyclic, successful structural counterexamples equal zero, Category 1 equals zero and fresh independent validation reaches its own PASS.

Findings use exactly four categories:

1. A declared invariant fails in a valid abstract history and can produce invalid PASS or `ADMISSION_ELIGIBLE` without first requiring failure of a legitimately deferred implementation mechanism: blocking.
2. Explicitly allocated physical implementation/runtime obligation: non-blocking for architecture-only closure.
3. Optional hardening beyond architectural correctness: non-blocking.
4. Documentation/non-material reconciliation unable to create invalid assurance or eligibility: non-blocking.

Validation may falsify the fixed boundary but cannot expand it. Production builds, registries, signatures, SBOM generation, environments, tests, telemetry, identity/access mechanisms, deployment authorization systems, deployment/rollback execution, monitoring, operational control, external actions and effect reconciliation are outside architecture-only certification.

Convergence requires all invariants PASS; all Material attack classes and applicable historical families exercised; Category 1 and successful counterexamples zero; exact mappings; one normative source; no competing authority; zero cycles, self-loops, future references, rank violations or unresolved nodes; independent PASS; and only Categories 2–4 remaining. Further test-count growth is optional unless a materially new falsification class appears.
